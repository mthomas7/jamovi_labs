data(nycflights)
library(tidyverse)
library(openintro)
data(nycflights)
nycflights
write.csv(nycflights, file="nycflights.csv")
?nycflights
sfo_feb_flights %>%
group_by(origin) %>%
summarise(median_dd = median(dep_delay), iqr_dd = IQR(dep_delay), n_flights = n())
